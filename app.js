function math(a,b) {
  console.log(`${a} + ${b} = `+ (a + b));
  console.log(`${a} - ${b} = `+ (a - b));
  console.log(`${a} * ${b} = `+ (a * b));
  console.log(`${a} ÷ ${b} = `+ (a / b));
}

math(3,4);
